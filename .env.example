############################
### Database Credentials ###
############################
DB_HOST=localhost
DB_PORT=3306
DB_NAME=your_database
DB_USER=your_user
DB_PASS=your_password


#################
### Storybook ###
#################
STORYBOOK_URL=https://your-storybook.vercel.app


####################
### Google OAuth ###
####################
AUTH_GOOGLE_ID=your-google-client-id
AUTH_GOOGLE_SECRET=your-google-client-secret
# Auth bypass token for debugging (server-side only)
# AUTH_BYPASS_TOKEN=dev-bypass


################
### NextAuth ###
################
# Use `openssl rand -base64 32` to generate AUTH_SECRET
AUTH_SECRET=generate-a-32-char-random-string
AUTH_URL=http://localhost:3000


###############
### General ###
###############
# When a local pipeline app runs in Docker and the dashboard runs on the host:
# DASHBOARD_BASE_URL=http://host.docker.internal:3000
DASHBOARD_BASE_URL=http://localhost:3000


###########################
### Process Integration ###
###########################
PIPELINE_API_BASE_URL=http://localhost:8000
# Use `openssl rand -base64 32` to generate the shared `dashboard -> pipeline-api` trigger token
PIPELINE_TRIGGER_TOKEN=generate-a-32-char-random-string
# Use `openssl rand -base64 32` to generate the shared `pipeline-api -> dashboard` callback token
PIPELINE_CALLBACK_TOKEN=generate-a-32-char-random-string

# Optional: limit the top-level ingest folder browser to specific Drive folder IDs
GOOGLE_INGEST_SOURCE_ROOT_FOLDER_IDS=["folder-id-1","folder-id-2"]

# Google Drive service account for the process page folder browser
# Use exactly one:
GOOGLE_SERVICE_ACCOUNT_FILE=/absolute/path/to/service_account.json
# The JSON has priority over the file path. Escape newlines in the private key.
GOOGLE_SERVICE_ACCOUNT_JSON='{"type":"service_account","project_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----","client_email":"..."}'

########################
### Exclusion Review ###
########################
# Required: Google Drive folder ID used as the exclusion-review tree root.
EXCLUSION_REVIEW_ROOT_FOLDER_ID=your-exclusion-review-root-folder-id
# Optional: allowlist of emails permitted to save include/exclude decisions.
EXCLUSION_REVIEW_ALLOWED_EMAILS=["reviewer@example.org"]
# Optional: number of direct children loaded per branch (default: 200).
EXCLUSION_REVIEW_CHILDREN_PAGE_SIZE=200
